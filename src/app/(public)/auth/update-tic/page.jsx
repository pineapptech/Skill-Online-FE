import UpdateTicForm from "@/components/auth/update-tic-form";
import React, { Suspense } from "react";

const UpdateTicPage = () => {
  return (
    <Suspense>
      <UpdateTicForm />
    </Suspense>
  );
};

export default UpdateTicPage;
